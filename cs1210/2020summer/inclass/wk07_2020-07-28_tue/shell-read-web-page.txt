Python 3.8.5 (tags/v3.8.5:580fbb0, Jul 20 2020, 15:57:54) [MSC v.1924 64 bit (AMD64)]
Type "help", "copyright", "credits" or "license" for more information.
import urllib.request
myURL = 'https://kevinlillis.github.io'
webPage = urllib.request.urlopen(myURL)

type(webPage)
<class 'http.client.HTTPResponse'>

L = list(webPage)

len(L)
56

L[0]
b'<!DOCTYPE html>\n'

# Save first line into a variable
firstLine = L[0]

firstLine
b'<!DOCTYPE html>\n'

type(firstLine)
<class 'bytes'>

# Look at the first character of this first line
firstLine[0]
60

# save first character into a variable
firstChar = firstLine[0]

firstChar
60

type(firstChar)
<class 'int'>

str(firstChar)
'60'

chr(firstChar)
'<'

# convert first line all at once
firstLine_str = firstLine.decode('utf-8')

firstLine_str
'<!DOCTYPE html>\n'


type(firstLine_str)
<class 'str'>

firstLine_str[0]
'<'

